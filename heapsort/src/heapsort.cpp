#include"heapsort.h"
HeapSort::HeapSort(vector<int> value)
{
    heapVector = value;
}
void HeapSort::maxHeapIfy(vector<int>value, int i)
{

}

void HeapSort::buildMaxHeap()
{
    if(heapVector.empty())
    {
        std::cout << "heapVector is empty!" << std::endl;
        return;
    }
}

vector<int> HeapSort::getHeapSort()
{

}